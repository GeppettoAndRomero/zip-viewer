# Test fixtures

`sample.heic` is generated from a synthetic gradient PNG (created in-repo, no third-party
image) and converted to HEIC with macOS `sips`. It carries no third-party copyright and is
safe to ship in this open-source repo. Regenerate with `tests/fixtures/heic/regenerate.sh`
(or the snippet in the test setup) if needed.
